extends layout

block content
  h1= title
  if category_list.length
    ul
      each category in category_list
        li
          a(href=category.url) #{category.name}
          |  (#{countItems(category, item_list)})

  else
    p There are no items.